artifacts:
  def_version: "1.0"

  example_training_dataset:
    type: fda.Bytes
    description: "An dummy dataset, used as an example to represent a data Artifact"
    major_version: "0"
  
  example_model:
    type: fda.Model
    description: "A dummy model, used as an example."
    major_version: "0"

  test_cr_parameters:
    type: fda.Bytes
    description: "Artifact to be consumed by the Bidder."
    major_version: "0"
    confidentiality: "public"
    ttl: 7
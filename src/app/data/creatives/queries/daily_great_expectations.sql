DECLARE TIME_ZONE STRING DEFAULT '{time_zone}';

SELECT a.*, CAST(hour AS INT64) AS int_hour
FROM meli-bi-data.SBOX_DSPCREATIVOS.BQ_PRINTS_CLICKS_CONVERSIONS_PER_HOUR a
WHERE ds >= EXTRACT(DATE FROM CURRENT_DATETIME(TIME_ZONE)) - 7
    AND site = '{site_id}';